
<section style={{overflowX: 'hidden'}}>
        <div class="textInfinite bg-color-blue-600">
            <h2>Diseño de Páginas y Sitios Web a medida</h2>
            <h2>Diseño de Páginas y Sitios Web a medida</h2>
        </div>
</section>


<style lang="scss">

    .textInfinite{
        display: flex;
        
        gap: 4rem;
        overflow-x: hidden;
        align-items: center;
        width: max-content;
        animation: loopText 10s forwards linear infinite;
        
        > h2{
            font-family: "Bebas";
            color: white;
            font-size: max(5rem, 25vmin);
            flex-shrink: 0;
            text-transform: uppercase;
            font-weight: 400;
            line-height: 1.5;
            letter-spacing: 2px;
            pointer-events: none;
        }

        @keyframes loopText {
            100%{
                 transform: translate(calc(-50% - 2rem));
            }
        }
    }
</style>